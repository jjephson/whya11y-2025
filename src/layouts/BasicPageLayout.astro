---
import ContentLayout from './ContentLayout.astro';

export interface Props {
	title: string;
	description: string;
	contentId?: string;
}

const { title, description, contentId = 'page-content' } = Astro.props;
---

<ContentLayout title={title} description={description} contentId={contentId}>
	<slot />
</ContentLayout>
